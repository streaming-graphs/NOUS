package gov.pnnl.aristotle.algorithms.PathSearch

object PathRanking {
/*
class Profile[T] (val pval: T) extends Serializable {
  def getProfile
}

class NodeProfile[T] (val label: String, val id: Long) extends Profile
class PredProfile[T] (val label: String) extends Profile
class EdgeProfile[T] (val srcId: Long, val dstId: Long, val predLabel: String) extends Profile
class PathProfile[T] (val path : List[PathEdge])
class QuestionModel {
  def train(trainingFile: String)
  def predict(q: String): (String, Int)
}

class DeepLearningModel {
  def train(trainingfile: String)
  def predict(testcase: String): Int
}

class Question(val q: String) {
  
  type InferredQuestion = String
  type InferredQuestionClassId = Int
  class MappedQuestion(val ques: InferredQuestion, val id : InferredQuestionClassId) 
 
  private def getMappedQuestion(questionModel : DeepLearningModel) : MappedQuestion = {
    questionModel.predict()
  }
  
  def getQuery(): Query = {
   
    val mappedQuestion: (String, questionClassId) = questionModel.mapNLPQuestion(q)
    val query : Query()
  }
}

class Query(val srclabel: String, val destLabel: String, val qContext: String)

class ProfiledEdge[T] (val profiledSrc: NodeProfile[T], val profiledDest: NodeProfile[T], val profiledPred: EdgeProfile[T])
*/
}